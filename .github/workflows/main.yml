name: Simulate commits on many repos

on:
  workflow_dispatch:
    inputs:
      repo_count:
        description: 'Number of demo repos to select'
        required: false
        default: 5
      repo_chance:
        description: '% chance each repo will get a commit+push'
        required: false
        default: 50

  #schedule: 
  #  - cron: '* * * * *' # Max, every 5 minutes

env:
  REPO_COUNT: ${{ inputs.repo_count }}
  REPO_CHANCE: ${{ inputs.repo_chance }}


jobs:
  set-n-repos:
    needs: []
    runs-on: ubuntu-latest
    outputs:
      repos: ${{ steps.set-repos.outputs.repos }}
    steps:
      - uses: actions/checkout@v6

      - name: Set repo names
        id: set-repos
        env:
          REPOS_FILENAME: repos.json
        run: |
          URL=https://raw.githubusercontent.com/kosli-demo/base/refs/heads/main/data/all-repos.json
          curl --fail --output temp.json --silent "${URL}"
          ./bin/select_repos.py temp.json ${REPO_COUNT} ${REPO_CHANCE} > "${REPOS_FILENAME}"
          REPOS="$(jq --compact-output . ${REPOS_FILENAME})"
          echo "repos=${REPOS}" >> ${GITHUB_OUTPUT}


  commit-to-n-repos:
    needs: [set-n-repos]
    if: ${{ needs.set-n-repos.outputs.repos != '[]' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include: ${{ fromJSON(needs.set-n-repos.outputs.repos) }}
    env:
      REPO_NAME: ${{ matrix.repo_name }}        
    steps:
      - name: Commit and push to repo
        run: |
          echo "REPO_NAME=:${REPO_NAME}:"
          gh repo create kosli-demo/${REPO_NAME} --public --clone || true
          pwd
          ls -al

      # cd repo
      # mv .git /tmp 
      # cd ..
      # clone base
      # cd base
      # rm -rf .git
      # mv /tmp/git .
      # MAKE A CHANGE
      # git ....

      # - name: Ensure repo exists
      #   env:
      #     GH_TOKEN: ${{ secrets.CLONE_PUSH_REPOS }}
      #   run:
      #     gh repo create kosli-demo/${REPO_NAME} --public || true

      # - uses: actions/checkout@v6
      #   with:
      #     repository: kosli-demo/${REPO_NAME}
      #     token: ${{ secrets.CLONE_PUSH_REPOS }}
      #     fetch-depth: 0
      #     ref: 'main'

      # - name: Checkout base repo
      #   run:
      #     echo replace matrix.repo_name's files with base repos files

      # - name: Make a change
      #   run: |
      #     pwd
      #     ls -al
      #     DATE_TIME="$(date +%Y-%m-%dT%H.%M.%S)"
      #     echo "${DATE_TIME}" >> .github/workflows/force-ci-run

      # - name: Commit and push
      #   env:
      #     GH_TOKEN: ${{ secrets.CLONE_PUSH_REPOS }}
      #   run: |
      #     git config --global user.name "${{ github.actor }}"
      #     git config --global user.email "${{ github.event.sender.login }}@users.noreply.github.com"
      #     git add .
      #     git commit --message "Testing spreader"
      #     git push
